---
import Header from '@/components/Header.astro'

interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html lang="pl" class="scroll-smooth bg-[#F4F3EE] text-[#1A1D20]">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="TK Flow — Technologie przesyłu cieczy. Centrala dystrybucji i doradztwa technicznego." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon-tk-flow.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet" />
  </head>
  <body class="bg-[#F4F3EE] font-sans antialiased text-[#1A1D20] selection:bg-[#1A1D20] selection:text-white overflow-x-hidden">
    <!-- Architektoniczna siatka tła -->
    <div class="fixed top-0 bottom-0 left-6 md:left-12 w-[1px] bg-[#E2E0D8] z-[-1] pointer-events-none"></div>
    <div class="fixed top-0 bottom-0 right-6 md:right-12 w-[1px] bg-[#E2E0D8] z-[-1] pointer-events-none"></div>

    <Header />
    <slot />
  </body>
</html>

<style is:global>
  ::selection {
    background-color: #1a1d20;
    color: #ffffff;
  }
  body {
    font-family: 'Inter', sans-serif;
  }
  h1, h2, h3, .font-display {
    font-family: 'Plus Jakarta Sans', sans-serif;
  }
  .kh-transition {
    transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  }
  ::-webkit-scrollbar {
    width: 6px;
  }
  ::-webkit-scrollbar-track {
    background: #f4f3ee;
    border-left: 1px solid #e2e0d8;
  }
  ::-webkit-scrollbar-thumb {
    background: #1a1d20;
  }
</style>